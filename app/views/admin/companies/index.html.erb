<div class="p-6">
  <h1 class="text-2xl font-bold mb-6 text-white bg-gradient-to-r from-blue-500 to-blue-400 p-4 rounded shadow-lg">
    Companies
  </h1>

  <%= link_to "New Company", new_admin_company_path, class: "px-4 py-2 bg-gradient-to-r from-blue-400 to-blue-600 text-white rounded shadow hover:opacity-90 transition mb-4 inline-block" %>

  <table class="w-full bg-white rounded shadow overflow-hidden">
    <thead class="bg-blue-500 text-white">
      <tr>
        <th class="px-4 py-2">Name</th>
        <th class="px-4 py-2">Actions</th>
      </tr>
    </thead>
    <tbody>
      <% @companies.each do |company| %>
        <tr class="border-b">
          <td class="px-4 py-2"><%= company.name %></td>
          <td class="px-4 py-2 space-x-2">
            <%= link_to "Edit", edit_admin_company_path(company), class: "px-2 py-1 bg-blue-400 text-white rounded hover:opacity-90 transition" %>
            <%= link_to "Delete", admin_company_path(company), method: :delete, data: { confirm: "Are you sure?" }, class: "px-2 py-1 bg-red-500 text-white rounded hover:opacity-90 transition" %>
            <%= link_to "Sectors", new_admin_company_sector_path(company), class: "px-2 py-1 bg-blue-600 text-white rounded hover:opacity-90 transition" %>
            <%= link_to "Managers", admin_company_users_path(company), class: "px-2 py-1 bg-blue-700 text-white rounded hover:opacity-90 transition" %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>